비트연산자에서

	<< 은 숫자가 커지는 방향이다.

	ex) 2 << 2 이면
	   10 이 1000이 된다는 말이다.

 a && b		a,b가 모두 true면 true
 a || b		a 또는 b가 treu면 true


메소드는 반드시 괄호가 있어야한다. 그러나 파라메터가 있어야
하는 것은 아니다.

멤버변수(heap)
지역변수(stack)
static변수(data)

호출(call) 시 메소드를 호출한다.

github

stackoverflow

String은 클래스를 객체화할 때 new를 사용안해도 된다!
ex) String name = "홍길동";과
    String name = new String("홍길동");은 같은 의미.

하나의 클래스에 여러가지 메소드를 갖고 있는 것을
"메소드 오버로딩"이라고 한다!

(기본)생성자는 쓸일이 없어도 만들어라!
		나중에 상속할 때 필요하기 때문!

다른 클래스에서 만들어진 멤버변수는, 메인 클래스에서 객체를 생성해야만 사용할 수 있다.
--------
	소스 - 컴파일 - 실행(JVM) - 
			jre(실행환경)안에 JVM이 있다. 
스태틱 변수는 멤버 메소드에 선언할 수 없다.

	"스태틱이 메인보다 먼저 선언된다."